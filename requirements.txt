# Core RL + environment
stable-baselines3>=2.0.0
gymnasium>=0.29.0
torch>=2.0.0

# Numerics, data, LP solver (HiGHS ships with scipy.optimize.linprog)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Figures
matplotlib>=3.7.0

# Tests
pytest>=7.4.0

# NOTE: for an exact-reproduction lock, replace the ranges above with a frozen set
# from the environment used to produce the paper results:  pip freeze > requirements.txt
